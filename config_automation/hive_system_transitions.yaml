- id: '1667928605956'
  description: 'Set state when transitioning system mode'
  trigger:
  - platform: state
    entity_id:
    - climate.heating_proxy
    id: heating proxy mode transition
    from: cool
    to: heat
  condition: []
  action:
  - service: input_text.set_value
    data:
      value: 'heat'
    target:
      entity_id: input_text.heating_current_system_mode
  mode: single